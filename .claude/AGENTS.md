<!--
Generated by ./scripts/sync-codex-agents.sh.
Source of truth: ac-cli-plugin/.claude/CLAUDE.md
Do not edit this file directly. Update the source file(s), then rerun the script.
-->

# Codex Instructions

Codex reads `AGENTS.md` automatically. This file mirrors the local Claude
instructions for Codex.

When instructions mention `CLAUDE.md`, treat this file as the Codex-native copy
and keep both generated from the same source.

# CLAUDE.md

<!-- Add your custom instructions below. Repowise will never modify anything outside the REPOWISE markers. -->
<!-- Examples: coding style rules, test commands, workflow preferences, constraints -->

<!-- REPOWISE:START — Do not edit below this line. Auto-generated by Repowise. -->
## IMPORTANT: Codebase Intelligence Instructions for ac-cli-plugin

> This repository is indexed by [Repowise](https://repowise.dev).
> Use the MCP tools below for orientation, discovery, and enriched context
> (documentation, ownership, history, decisions). **Always verify against
> actual source files before making changes** — the index may be stale.

Last indexed: 2026-05-27 (commit 06e55e4). Confidence: 100%.
### Architecture
This repository is a Claude AI plugin marketplace: it consumes structured skill definitions (Markdown reference docs, JSON evaluation suites, and plugin manifests) as inputs, organises them through a plugin/skill authoring framework governed by Claude rules, and produces deployable Claude plugins — specifically the ac-cli plugin — that teach Claude how to operate the ActiveCampaign CLI tool, validated end-to-end by an automated evaluation harness (run.py) against a defined set of test cases (evals.json). The repository serves two interrelated purposes:
1. **Plugin authoring infrastructure** — conventions, rules, and tooling for building Claude plugins that extend Claude's capabilities with domain-specific knowledge. **The ac-cli plugin** — a concrete, production plugin that grounds Claude in ActiveCampaign's CLI commands, CRM concepts, platform workflows, authentication patterns, and admin operations.
### Entry Points
- `plugins/ac-cli/skills/ac-cli/evals/run.py`
### Architectural Layers
| Layer | Files | Purpose |
|-------|-------|---------|
| Agent Guidelines | 1 | Documents defining behavioral rules and conventions for AI agents interacting wi |
| Claude Configuration | 1 | Configuration and instructions file tailored for the Claude AI assistant's inter |
| Project Overview | 1 | Top-level README providing an introduction, setup instructions, and usage docume |
| Plugin Structure Rules | 1 | Authoring rules defining the required structure and conventions for plugin organ |
| Skill Authoring Rules | 1 | Guidelines specifying how to write and structure skills within the plugin framew |
| Plugin Manifest | 1 | Defines the ac-cli plugin metadata and configuration for the Claude plugin syste |
| Marketplace Config | 1 | Defines the marketplace listing configuration for publishing the plugin to the C |
| AC-CLI Skill Docs | 12 | Contains the skill definition, evaluation harness, and reference documentation f |
| Version Bump Script | 1 | Shell script for automating version bumping across the project. |

### Guided Tour (10 steps)
1. **Project Overview & Agent Guidelines** — `README.md`
2. **Claude AI Configuration** — `CLAUDE.md`
3. **Plugin Structure & Skill Authoring Rules** — `.claude/rules/01-plugin-structure.md`
4. **Plugin Manifest & Marketplace Config** — `plugins/ac-cli/.claude-plugin/plugin.json`
5. **AC-CLI Skill Definition** — `plugins/ac-cli/skills/ac-cli/SKILL.md`
6. **AC-CLI Reference Documentation** — `plugins/ac-cli/skills/ac-cli/references/commands.md`
... and 4 more steps

## Code health
Hotspot health: 4.13/10 (stable) ·
Average: 4.13/10 ·
Worst: 3.72/10 (`plugins/ac-cli/skills/ac-cli/evals/run.py`)

### Repowise MCP Tools

This repo has the Repowise MCP server configured. The tools below answer questions `grep`/`Read` cannot. Every response carries an `_meta` envelope with `index_age_days`, `indexed_commit`, and a `stale_warning` only when the index has actually diverged from HEAD — silence means the index is current.

**When to call which tool:**

| Tool | What only this tool answers |
|------|------------------------------|
| `get_answer(question)` | Synthesised answer with verified citations and a calibrated `retrieval_quality`. First call for "how does X work" / "why is Y like this". On low confidence returns `best_guesses` with one-line justifications instead of an empty answer. |
| `get_context(targets=[...])` | Triage card for files/modules/symbols — title, summary, signatures, `hotspot` bit, `decision_records` titles, and `symbol_id`s to pipe into `get_symbol`. Use `include=["callers","ownership",...]` to widen. NOT for source bytes. |
| `get_symbol("path/to/file.py::Name")` | Raw source bytes for one indexed symbol with exact line bounds. Cheaper and safer than `Read` + offset math. Use the `symbol_id` returned by `get_context`. |
| `search_codebase(query, kind?)` | Find pages by concept when you don't know the file. Each result carries `search_method` (`embedding` vs `bm25` fallback). For exact identifiers use Grep — the tool will hint when it sees one. |
| `get_why(query, targets?)` | Architectural decision archaeology — *why* the code is shaped this way. Call before refactors or pattern divergences. Falls back to git archaeology when no ADRs exist for a file. |
| `get_risk(targets, changed_files?)` | What history says about touching these files: churn, owners, blast radius. Pass `changed_files` for PR mode → returns a `directive` (`will_break`, `missing_cochanges`, `missing_tests`). |
| `get_dead_code(...)` | Tiered unreachable / unused-export / zombie-package findings. Run before a cleanup sprint, not before a targeted fix. |
| `get_overview(repo?)` | Architecture map for an unfamiliar repo. One-time orientation; skip on subsequent calls in the same session. |

**Composition tips:**
- `get_answer` → if `confidence` is `medium`/`low`, follow the `best_guesses[0].file` or `fallback_targets[0]` into `get_context`, then `get_symbol` for bytes.
- `get_context` returns `decision_records` titles → call `get_why(targets=[...])` for the rationale.
- `get_context` returns `hotspot: true` → call `get_risk` before editing.
- PR review → `get_risk(targets=[...], changed_files=[...])`; read the `directive` block first.

**Verify when:** `_meta.stale_warning` is present, or `retrieval_quality` is `partial`/`weak`, or `search_method` is `bm25`. Otherwise trust the response and act on it.

<!-- REPOWISE:END -->
